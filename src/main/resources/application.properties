# MongoDB URI — replace 'localhost' with your actual MongoDB connection string
spring.data.mongodb.uri=mongodb+srv://Ajay67:Ajay0507.@cluster0.78xqt7k.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Redis — only valid if you're deploying Redis (e.g. on RedisCloud or Upstash)
spring.data.redis.host=noted-magpie-35259.upstash.io
   # e.g., redis-12345.c12.us-east-1-4.ec2.cloud.redislabs.com
spring.data.redis.port=6379  
# usually 6379 or different in cloud

# App Port — 8080 is fine, Render auto-detects this
server.port=8080

# Logging
logging.level.org.springframework.web=INFO      # DEBUG is too noisy for production
logging.level.com.kash.RealTimeChat=INFO

# JWT Secret — keep this safe and never hard-code in production!
jwt.secret=${JWT_SECRET}
